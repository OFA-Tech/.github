name: Stage - Verify

on:
  workflow_call:
    inputs:
      language:
        description: Programming language (dotnet, node, java, python, go, php, cpp)
        required: true
        type: string
      language-version:
        description: Runtime version
        required: false
        type: string
        default: ""
      distribution:
        description: Optional Java distribution override
        required: false
        type: string
        default: ""
      working-directory:
        description: Working directory for commands
        required: false
        type: string
        default: .
      runner:
        description: Runner label
        required: false
        type: string
        default: ubuntu-latest
      run-build:
        description: Enables build step
        required: false
        type: boolean
        default: true
      build-command:
        description: Optional build command override
        required: false
        type: string
        default: ""
      run-tests:
        description: Enables test step
        required: false
        type: boolean
        default: true
      run-unit-tests:
        description: Enables unit test step
        required: false
        type: boolean
        default: true
      unit-test-command:
        description: Optional unit test command override
        required: false
        type: string
        default: ""
      run-integration-tests:
        description: Enables integration test step
        required: false
        type: boolean
        default: false
      integration-test-command:
        description: Optional integration test command override
        required: false
        type: string
        default: ""
      run-mutation-tests:
        description: Enables mutation test step
        required: false
        type: boolean
        default: false
      mutation-test-command:
        description: Optional mutation test command override
        required: false
        type: string
        default: ""
      test-command:
        description: Optional test command override
        required: false
        type: string
        default: ""
      run-sonar:
        description: Enables SonarQube step
        required: false
        type: boolean
        default: false
      sonar-project-key:
        description: Optional SonarQube project key override
        required: false
        type: string
        default: ""
      sonar-project-name:
        description: Optional SonarQube project name override
        required: false
        type: string
        default: ""
      sonar-sources:
        description: Sonar sources path
        required: false
        type: string
        default: .
      sonar-command:
        description: Optional custom Sonar command
        required: false
        type: string
        default: ""
      enforce-quality-gate:
        description: Fail stage if quality gate is red
        required: false
        type: boolean
        default: false
      sonar-token:
        description: Optional Sonar token override
        required: false
        type: string
        default: ""
      sonar-host-url:
        description: Optional Sonar host URL override
        required: false
        type: string
        default: ""
      upload-artifact:
        description: Upload artifact after build verification
        required: false
        type: boolean
        default: false
      artifact-name:
        description: Artifact name
        required: false
        type: string
        default: build-output
      artifact-path:
        description: Artifact path or glob
        required: false
        type: string
        default: .
    secrets:
      sonar-token:
        required: false
      sonar-host-url:
        required: false

jobs:
  verify:
    runs-on: ${{ inputs.runner }}
    permissions:
      contents: read
    env:
      RESOLVED_SONAR_TOKEN: ${{ secrets.sonar-token }}
      RESOLVED_SONAR_HOST_URL: ${{ secrets.sonar-host-url }}
      RESOLVED_SONAR_PROJECT_KEY: ${{ inputs.sonar-project-key }}
      RESOLVED_SONAR_PROJECT_NAME: ${{ inputs.sonar-project-name }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Resolve language defaults
        id: defaults
        uses: OFA-TECH/.github/actions/setup/resolve@main
        with:
          language: ${{ inputs.language }}
          version: ${{ inputs.language-version }}
          distribution: ${{ inputs.distribution }}
          build-command: ${{ inputs.build-command }}
          test-command: ${{ inputs.test-command }}

      - name: Setup runtime
        uses: OFA-TECH/.github/actions/setup/runtime@main
        with:
          language: ${{ steps.defaults.outputs.language }}
          version: ${{ steps.defaults.outputs.version }}
          distribution: ${{ steps.defaults.outputs.distribution }}

      - name: Build
        if: ${{ inputs.run-build && steps.defaults.outputs.build-command != '' }}
        uses: OFA-TECH/.github/actions/run-command@main
        with:
          command: ${{ steps.defaults.outputs.build-command }}
          working-directory: ${{ inputs.working-directory }}

      - name: Run unit tests
        if: ${{ inputs.run-tests && inputs.run-unit-tests && (inputs.unit-test-command != '' || steps.defaults.outputs.test-command != '') }}
        uses: OFA-TECH/.github/actions/run-command@main
        with:
          command: ${{ inputs.unit-test-command != '' && inputs.unit-test-command || steps.defaults.outputs.test-command }}
          working-directory: ${{ inputs.working-directory }}

      - name: Run integration tests
        if: ${{ inputs.run-tests && inputs.run-integration-tests && inputs.integration-test-command != '' }}
        uses: OFA-TECH/.github/actions/run-command@main
        with:
          command: ${{ inputs.integration-test-command }}
          working-directory: ${{ inputs.working-directory }}

      - name: Run mutation tests
        if: ${{ inputs.run-tests && inputs.run-mutation-tests && inputs.mutation-test-command != '' }}
        uses: OFA-TECH/.github/actions/run-command@main
        with:
          command: ${{ inputs.mutation-test-command }}
          working-directory: ${{ inputs.working-directory }}

      - name: Validate Sonar configuration
        if: ${{ inputs.run-sonar && (env.RESOLVED_SONAR_TOKEN == '' || env.RESOLVED_SONAR_HOST_URL == '' || env.RESOLVED_SONAR_PROJECT_KEY == '') }}
        shell: bash
        run: |
          echo "Missing Sonar configuration. Ensure token, host URL and project key are available via inputs, secrets, or org/repo vars." >&2
          exit 1

      - name: Run custom Sonar command
        if: ${{ inputs.run-sonar && inputs.sonar-command != '' }}
        uses: OFA-TECH/.github/actions/run-command@main
        with:
          command: ${{ inputs.sonar-command }}
          working-directory: ${{ inputs.working-directory }}
        env:
          SONAR_TOKEN: ${{ env.RESOLVED_SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ env.RESOLVED_SONAR_HOST_URL }}

      - name: Run Sonar scan action
        if: ${{ inputs.run-sonar && inputs.sonar-command == '' }}
        uses: SonarSource/sonarqube-scan-action@v6
        env:
          SONAR_TOKEN: ${{ env.RESOLVED_SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ env.RESOLVED_SONAR_HOST_URL }}
        with:
          projectBaseDir: ${{ inputs.working-directory }}
          args: >-
            -Dsonar.projectKey=${{ env.RESOLVED_SONAR_PROJECT_KEY }}
            -Dsonar.projectName=${{ env.RESOLVED_SONAR_PROJECT_NAME }}
            -Dsonar.sources=${{ inputs.sonar-sources }}

      - name: Enforce quality gate
        if: ${{ inputs.run-sonar && inputs.enforce-quality-gate && inputs.sonar-command == '' }}
        uses: SonarSource/sonarqube-quality-gate-action@v1.2.0
        timeout-minutes: 5
        env:
          SONAR_TOKEN: ${{ env.RESOLVED_SONAR_TOKEN }}

      - name: Upload build artifact
        if: ${{ inputs.upload-artifact }}
        uses: actions/upload-artifact@v4
        with:
          name: ${{ inputs.artifact-name }}
          path: ${{ inputs.artifact-path }}
